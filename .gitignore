# env and local config
.env
rag/vaults.json
rag/routing.json
scripts/anonymization-patterns.local.txt

# rust
target/

# downloaded embedding model cache (fallback location if EOS_MODEL_CACHE unset
# and an older build wrote here)
.fastembed_cache/

# index artifacts
*.db
*.db-journal

# editors / os
.DS_Store
.idea/
.vscode/

# obsidian workspace state (keep vault-template/.obsidian minimal and committed)
vault-template/.obsidian/workspace*.json
vault-template/.obsidian/cache
