---
type: candidate-fact
source: example session
evidence: "let's use uv for the python side, not pip"
status: example
---

# Candidate: prefers uv over pip for Python (example)

Example staged candidate. An agent noticed this mid-work and dropped it here
for review rather than writing it into the curated environment note directly.

Promote by moving the fact into `environment/local-setup.md`, then delete this
file.
