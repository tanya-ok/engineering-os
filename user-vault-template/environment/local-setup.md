---
type: environment
status: template
---

# My local environment

Replace with your own. This lets the agent write commands and examples that
actually run on your machine instead of generic ones.

## Shell and OS

- OS: (e.g. macOS / Linux)
- Shell: (e.g. fish / zsh / bash) - write examples in this syntax.

## Toolchain

- Language runtimes and versions: (e.g. Node 24 LTS, Python 3.12).
- Package managers: (e.g. pnpm, uv).
- Linter / formatter: (e.g. Biome, ruff).

## Local utilities and conventions

- (example) A token-optimizing CLI proxy is installed; prefer it for dev ops.
- (example) Secrets come from a secret manager at runtime, never committed.
- (example) Repos live under ~/code; new work branches off main.
