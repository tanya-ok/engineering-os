# onnxruntime-node downloads CUDA execution-provider binaries on Linux at
# install time. Embedding runs on CPU here, so skip the download; it is also
# what made CI time out.
onnxruntime-node-install=skip
