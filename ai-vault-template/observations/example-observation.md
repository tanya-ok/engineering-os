---
type: observation
status: example
date: 2026-01-01
---

# Observation: the user reviews before any publish (example)

Example note. This is what an agent writes to its own memory after a session.
Delete once you have real observations.

## What happened

The user asked for a second review pass before any push to a public remote,
in every repo, not just the first time.

## What I inferred

Publishing is the irreversible boundary for this user. Local commits are fine
unprompted; the remote is a hard gate.

## Promotion

This repeated a prior session's correction, so I promoted it to a standing
rule in `interaction-rules/working-with-the-user.md`.
